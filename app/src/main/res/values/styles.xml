<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="ToolbarStyle" parent="@style/AppTheme">
        <item name="colorAccent">@color/colorActivityBack</item>
    </style>

    <style name="BlueAccent" parent="@style/AppTheme">
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

    <style name="BlackAccent" parent="@style/AppTheme">
        <item name="colorAccent">#181818</item>
    </style>

</resources>
